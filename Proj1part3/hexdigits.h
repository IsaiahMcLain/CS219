#ifndef HEXDIGITS_H
#define HEXDIGITS_H

#include<iostream>

//Struct that holds the hexadeimal operands as decimals for later manipulation
struct HexDigits {
    uint32_t hexVal1;
    uint32_t hexVal2;
};

#endif //HEXDIGITS_H